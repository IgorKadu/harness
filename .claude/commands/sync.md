---
description: Reescreve o state-of-world (memoria quente) e mede o CORE.
---
Chame a tool MCP `os_sync`. Depois, se o foco do projeto mudou, atualize o bloco
"Onde paramos" em `.ai/memory/state-of-world.md` (reescrevendo, nao append).
Confirme que `os_doctor` segue OK.
