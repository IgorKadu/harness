---
name: init
description: Harness Lean AI OS — prepara o OS (projeto novo ou existente).
---
1. `os_doctor` para validar a integridade do OS.
2. NOVO: dialogue p/ entender o objetivo e monte a estrutura incrementalmente; decisoes -> ADR + `os_remember decisions`.
3. EXISTENTE: escaneie o estado atual e alinhe o `state-of-world.md` ao que existe e ao que vem a seguir.
4. Sem pre-carregar tudo: o `retrieval-index.json` cresce junto com o projeto.
